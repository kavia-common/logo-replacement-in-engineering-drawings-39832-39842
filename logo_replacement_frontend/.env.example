# Frontend environment example
# Preferred backend base URL for API calls. If omitted, the app falls back to same-origin or CRA proxy.
REACT_APP_BACKEND_URL=http://localhost:3001

# Legacy support (still honored if set)
# REACT_APP_API_BASE=http://localhost:3001
